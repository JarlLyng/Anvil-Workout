# Xcode
*.xcodeproj/xcuserdata/
*.xcodeproj/project.xcworkspace/xcuserdata/
DerivedData/
*.pbxuser
*.mode1v3
*.mode2v3
*.perspectivev3
*.moved-aside
*.hmap
*.ipa
*.dSYM.zip
*.dSYM
xcuserdata/

# Secrets
Iron Workout/Config/Secrets.xcconfig

# macOS
.DS_Store
**/.DS_Store

# Screenshots (App Store assets — large binaries, not source code)
/Screenshots/
/app store screens/

# AI tooling stays local (#87). The repo is indifferent to which AI is used and should not
# smell of tooling: AGENTS.md is the one agent-context file, and everything a specific
# editor or agent writes stays on the machine that wrote it. From the hub's _template/.gitignore.
.claude/
CLAUDE.md
CLAUDE.local.md
.cursor/
.cursorrules
.windsurfrules
.clinerules
.aider*
.continue/
.codex/
.mcp.json
GEMINI.md
.github/copilot-instructions.md
